local Players = game:GetService("Players")
local player = Players.LocalPlayer
local char = player.Character or player.CharacterAdded:Wait()
local hrp = char:WaitForChild("HumanoidRootPart")

-- 1. TELEPORT TỚI TỌA ĐỘ YÊU CẦU KHI CHẠY SCRIPT
local targetPos = Vector3.new(-80, 102, 265)
hrp.CFrame = CFrame.new(targetPos)

print("QuacHub: Đã Teleport tới tọa độ yêu cầu!")

-- 2. VÒNG LẶP QUÉT MÙ TOÀN BỘ WORKSPACE (THẤY GÌ HÚT NẤY)
local SCAN_DELAY = 0 -- Tốc độ quét

task.spawn(function()
    while true do
        pcall(function()
            -- Cập nhật lại HRP nếu nhân vật chết hoặc reset
            local currentChar = player.Character
            local currentHrp = currentChar and currentChar:FindFirstChild("HumanoidRootPart")
            
            if currentHrp then
                local allObjects = workspace:GetDescendants()
                
                for i = 1, #allObjects do
                    local obj = allObjects[i]
                    
                    -- Tìm bất kỳ cái gì có TouchInterest
                    if obj:IsA("TouchTransmitter") then
                        local parentPart = obj.Parent
                        
                        -- Nếu là Part hợp lệ thì Fire
                        if parentPart and parentPart:IsA("BasePart") then
                            firetouchinterest(currentHrp, parentPart, 0) -- Chạm
                            firetouchinterest(currentHrp, parentPart, 1) -- Thả
                        end
                    end
                end
            end
        end)
        task.wait(SCAN_DELAY)
    end
end)

print("QuacHub: Auto Scan All đang chạy...")
        end)
        
        if not success then
            warn("Lỗi quét Orb: " .. err)
        end
        
        task.wait(WAIT_TIME)
    end
end)
