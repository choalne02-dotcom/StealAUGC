if getgenv().AfkForFreeUGC_bwush then return end
getgenv().AfkForFreeUGC_bwush = true

--===[ QueueOnTeleport Loader ]===--
local QueueOnTeleport = queue_on_teleport or syn.queue_on_teleport
local SCRIPT_URL = "https://raw.githubusercontent.com/QuangTrinh1009200303012007/RobloxUGC_Scripts/refs/heads/main/AfkForFreeUGC_bwush"

pcall(function()
    if QueueOnTeleport then
        QueueOnTeleport("loadstring(game:HttpGet('"..SCRIPT_URL.."'))()")
    end
end)

-- Đợi Game Load Xong 100%
if not game:IsLoaded() then
    game.Loaded:Wait()
end

-- Khai báo vị trí
local targetPos = Vector3.new(741, 259, -329)
local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local hrp = character:WaitForChild("HumanoidRootPart")

-- 1. Thực hiện Teleport
hrp.CFrame = CFrame.new(targetPos)

-- 2. Kiểm tra xem đã đến đích chưa (Vòng lặp chờ check khoảng cách)
repeat
    task.wait(0.5)
    hrp.CFrame = CFrame.new(targetPos) -- Force lại vị trí nếu bị trôi
until (hrp.Position - targetPos).Magnitude < 5

-- Đợi thêm một chút cho dữ liệu ổn định
task.wait(2)

-- 3. Thiết lập tự động vào lại khi bị Kick (Rejoin)
game:GetService("GuiService").ErrorMessageChanged:Connect(function()
    task.wait()
    game:GetService("TeleportService"):Teleport(game.PlaceId, player)
end)

-- 4. Thực hiện Kick để kích hoạt Rejoin
player:Kick("Rejoining to activate script...")
